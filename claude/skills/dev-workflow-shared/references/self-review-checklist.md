# 自己レビューチェックリスト

`bug-investigation-workflow`/`implementation-workflow`の「実装・自己レビュー」から参照される、diffレビュー時の観点。

- 意図しない変更(デバッグ用の記述の残存、無関係な差分など)が混入していないか
- 過剰な抽象化・不要なエラーハンドリングが混入していないか
- コメントが「why」以外(コードから読み取れる「what」の説明など)を長々と書いていないか
