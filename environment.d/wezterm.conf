# wezterm-sessionsプラグインが自動保存(10秒ごと)のたびに全ペインの情報をlog_infoで吐き、
# GUIログ(tmpfs)とjournal(GNOMEがstderrを流し込む)が日に百数十MB単位で肥大化するため、
# Luaからのログ(target: logging)だけWARN以上に絞る。WezTerm本体のINFOはそのまま残す
WEZTERM_LOG=info,logging=warn
